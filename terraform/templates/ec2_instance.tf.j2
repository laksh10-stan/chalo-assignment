output "primary_instance_ip" {
  description = "The public IP of the primary PostgreSQL instance."
  value       = aws_instance.primary.public_ip
}

output "replica_instance_ips" {
  description = "The public IPs of the replica PostgreSQL instances."
  value       = aws_instance.replicas[*].public_ip
}
